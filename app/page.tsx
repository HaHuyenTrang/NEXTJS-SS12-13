import React from 'react'

export default function page() {
  return (
    <div>page
      {/* 
         reactjs
         cơ chế render dữ liệu reactjs
         nextjs 
         hỗ trợ vừa render dữ liệu ở phía client và phía server
         NEXTJS:
         1. server component: mặc định các em tạo trang!
         2. client component : dùng "use client" --- cần tương tác dữ liệu (use state, use effect...., các sự kiện)

         cách render dữ liệu phía server 

      */}
    </div>
  )
}
